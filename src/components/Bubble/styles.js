import { StyleSheet, css } from 'aphrodite';

const styles = StyleSheet.create({
  bubble: {
    textAlign: 'left',
    maxWidth: '80%',
    minWidth: '20%'
  },
  dir0: {
    backgroundColor: '#eee'
  },
  dir1: {
    backgroundColor: '#ddd'
  }
});

export { styles, css };

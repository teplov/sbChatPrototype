import { StyleSheet, css } from 'aphrodite';

const styles = StyleSheet.create({
  avatar: {
    borderRadius: '100%',
    marginRight: '10px',
    width: '50px',
    height: '50px',
    backgroundColor: '#ccc',
    backgroundSize: 'cover'
  }
});

export { styles, css };
